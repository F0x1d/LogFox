# LogFox

Android application used to read system logcat using adb, shizuku or root access

## Gradle

### Modularization

App is modularized using core and feature modules

TYPESAFE_PROJECT_ACCESSORS are being used

### Tasks

Run ALL gradle tasks with `--quiet` flag
